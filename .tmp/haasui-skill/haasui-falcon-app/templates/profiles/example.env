# Copy to profiles/<profile-id>.env and fill only values verified from the
# target Youdao Dictionary Pen. Do not commit credentials or private URLs.

PLUGIN_NAME=myplugin
TARGET_LIB_DIR=libs/REPLACE_WITH_AIOT_TARGET_DIR
CROSS_TOOLCHAIN_PREFIX=/opt/toolchains/REPLACE/bin/REPLACE-
TOOLCHAIN_SETUP_COMMAND=echo REPLACE_WITH_REPRODUCIBLE_TOOLCHAIN_SETUP
UI_DIR=.
NATIVE_DIR=jsapi
PACKAGE_SCRIPT=build:prod
